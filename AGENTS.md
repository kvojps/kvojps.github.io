# kvojps.github.io

Landing page pessoal de José Ferreira. HTML/CSS estático servido pelo GitHub Pages a partir da
branch `main`. Sem build, sem dependências: o site inteiro são `index.html` e `style.css`.

## Agent skills

### Issue tracker

Issues e specs vivem como markdown em `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Os cinco papéis canônicos, sem renomeação, mais um `done` local. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` e `docs/adr/` na raiz. See `docs/agents/domain.md`.
